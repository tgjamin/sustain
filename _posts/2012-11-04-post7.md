---
layout: post
title: Template blog post
tags:
- News
- Tags
- Blog
- Post
---

# Some header


Some code:

```
import binascii

f = open("ID100302trackerARM7.hex")
w = open("dump.bin", "w+")

for line in f:
  hex_string = line.strip(":").strip("\n").strip("\r")
    binary_string = binascii.unhexlify(hex_string)
      w.write(binary_string)

```
